export enum Snapshot {
  Init = 'init',
  Import = 'import',
  Update = 'update',
  Add = 'add',
  Remove = 'remove',
  CreateArea = 'createArea',
  CreateSection = 'createSection',
  CreateRow = 'createRow',
  CreateSeat = 'createSeat',
}

export enum SeatMapType {
  Client = 'client',
  Event = 'event',
  Create = 'create',
  View = 'view',
}

export enum RowLabelAt {
  Both = 'both',
  Start = 'start',
  End = 'end',
  None = 'none',
}
